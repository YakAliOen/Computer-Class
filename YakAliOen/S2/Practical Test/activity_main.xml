<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F2D2BD">

    
    <!-- Header Logo -->
    <ImageView
        android:id="@+id/logoHeader"
        android:layout_width="100dp"
        android:layout_height="60dp"
        android:src="@drawable/Atlas"
        android:scaleType="fitCenter"
        android:layout_marginStart="12dp"
        android:layout_marginTop="12dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />


    <!-- NB2 Hero Section -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/NB2HERO"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#C4A484"
        app:layout_constraintTop_toBottomOf="@id/logoHeader"
        app:layout_constraintBottom_toTopOf="@id/ADHERO"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/temporary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Welcome to"
            android:textColor="@android:color/black"
            android:textSize="18sp"
            android:textStyle="bold|italic"
            android:layout_margin="24dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    
    <!-- AD Hero Section -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/ADHERO"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#988558"
        app:layout_constraintTop_toBottomOf="@id/NB2HERO"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
